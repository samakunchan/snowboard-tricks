{{ form_start(form, {'attr': {'id': 'form'}}) }}
    <div class="form-row">
        <div class="col">
            <!-- First name -->
            <div class="md-form">
                {{ form_widget(form.content, { 'attr': {'class': 'form-control'} }) }}
                <label for="register_pseudo">Content</label>
            </div>
        </div>
    </div>
    <button class="btn btn-xs text-right">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
